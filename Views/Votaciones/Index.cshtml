@model IEnumerable<Califi_restaurante.Models.tbl_Votaciones>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id_restaurante)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id_dato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Votacion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id_restaurante)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id_dato)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Votacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_votacion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_votacion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_votacion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
